
(defpackage #:getx
  (:use
   #:common-lisp)
  (:export
   ? ?? p?
   all*
   associate
   call
   foreach*
   each-key*
   each-value*
   except
   either
   filter
   keep*
   keep-type*
   multiple
   multiple-values
   progn?
   seek
   select
   select*
   yield
   fmt
   suppose))
