
(defpackage #:getx
  (:use
   #:common-lisp)
  (:export
   ? ?? p?
   *standard-object-plist*
   all
   also
   associate
   call
   call*
   fork
   each-key
   each-value
   except
   either
   filter
   keep
   listing
   listing*
   multiple-values
   progn?
   seek
   select
   unselect
   thereis
   yield
   fmt
   suppose
   join
   pick
   seq
   cat
   default
   else
   else!
   orelse
   affirm
   is
   with-bindings))
